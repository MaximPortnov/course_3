Процедура ВывестиМассив(Массив)
    Сообщить("");
    Для Каждого Элемент Из Массив Цикл
        Сообщить(Элемент);
    КонецЦикла;
КонецПроцедуры;

// Создание массива
МассивЧисел = Новый Массив;

// Заполнение массива
МассивЧисел.Добавить(1);
МассивЧисел.Добавить(2);
МассивЧисел.Добавить(3);
МассивЧисел.Добавить(4);

// Вывод массива с использованием цикла for
Для Индекс = 0 По МассивЧисел.Количество() - 1 Цикл
    Сообщить(МассивЧисел[Индекс]);
КонецЦикла;
Сообщить("");      
// Удаление элемента
МассивЧисел.Удалить(2); // Удаление элемента с индексом 2
// Вывод массива с использованием цикла ForEach
Для Каждого Элемент Из МассивЧисел Цикл
    Сообщить(Элемент);
КонецЦикла;
МассивЧисел.Вставить(1, 5);   
ВывестиМассив(МассивЧисел);




